spring.application.name=Inventory-Mangement
spring.profiles.active=dev

# JWT (shared across profiles; override per-profile if needed)
jwt.secret=change-this-super-secret-key-inventory-management-jwt-signing-key-32bytes-min
jwt.expiration-ms=86400000